diff --git a/dist/ui/utils/convertMessage.js b/dist/ui/utils/convertMessage.js
index 1610cf0209138cf59a14d26c2a787edd14a7ee41..5c45a8d2501a47f2a29f40427dcac6c7c5105394 100644
--- a/dist/ui/utils/convertMessage.js
+++ b/dist/ui/utils/convertMessage.js
@@ -107,9 +107,9 @@ function convertParts(message, metadata) {
                 // strip closing delimiters added by the AI SDK's fix-json
                 argsText = stripClosingDelimiters(argsText);
             }
-            else {
-                metadata.toolArgsKeyOrderCache?.delete(argsKeyOrderCacheKey);
-            }
+            // NOTE: Do NOT delete from toolArgsKeyOrderCache here. Deleting during render
+            // causes argsText to lose stabilized key order on subsequent renders, leading
+            // to duplicate toolCallId crash in tapResources (see cubic-dev-ai PR #3428 review)
             const toolStatus = metadata.toolStatuses?.[toolCallId];
             return {
                 type: "tool-call",
diff --git a/src/ui/utils/convertMessage.ts b/src/ui/utils/convertMessage.ts
index 0a10612dc38ff8b2ee833881f9dfe6722e97e9bb..2b2704c1832262cf438d2b9baa999972aea796ae 100644
--- a/src/ui/utils/convertMessage.ts
+++ b/src/ui/utils/convertMessage.ts
@@ -169,9 +169,10 @@ function convertParts(
         if (part.state === "input-streaming") {
           // strip closing delimiters added by the AI SDK's fix-json
           argsText = stripClosingDelimiters(argsText);
-        } else {
-          metadata.toolArgsKeyOrderCache?.delete(argsKeyOrderCacheKey);
         }
+        // NOTE: Do NOT delete from toolArgsKeyOrderCache here. Deleting during render
+        // causes argsText to lose stabilized key order on subsequent renders, leading
+        // to duplicate toolCallId crash in tapResources (see cubic-dev-ai PR #3428 review)
 
         const toolStatus = metadata.toolStatuses?.[toolCallId];
         return {
