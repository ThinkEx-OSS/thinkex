/**
 * Single source of truth for the composer file picker (`accept`) and
 * `SupabaseAttachmentAdapter.accept`. Keep aligned with server upload rules when changing.
 */
export const COMPOSER_ATTACHMENT_ACCEPT =
  "image/*,.pdf,.doc,.docx,.xls,.xlsx,.ppt,.pptx,.txt,.md,.csv,.json,.heic,.heif,.avif,.tiff,.tif";
